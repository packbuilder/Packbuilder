services:
  database:
    image: postgres:17
    ports:
      - "5432:5432"
    volumes: 
      - packbuilder-data:/var/lib/postgres
    environment:
      - POSTGRES_USERNAME=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=packbuilder
  
volumes: 
    packbuilder-data: